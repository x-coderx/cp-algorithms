#include<bits/stdc++.h>
using namespace std;
int main()
{
    int t;
    cin>>t;
    while(t--)
    {
        string s;
        int n,mid=n/2,count=0;
        cin>>n>>s;
        for(int i=0;i<mid;i++)
        {
            if(s[i]==')')
            {
                mid++;
                count++;
            }
        }
        for(int i=mid;i<n;i++)
        {
            if(s[i]==)
        }
    }
}

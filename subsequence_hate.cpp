#include<bits/stdc++.h>
using namespace std;
int main()
{
    int t;
    cin >> t;
    while (t--)
    {
        string s;
        cin >> s;
        if (s.size() < 3)
        {
            cout << 0 << endl;
            continue;
        }
        for (int i = 0; i < s.size() - 1; i++)
        {
            if (s[i] - '0' & s[i+1] - '0')

        }
    }
    return 0;
}
